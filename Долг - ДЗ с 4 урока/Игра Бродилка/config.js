/**
 * объект с настройками игры
 * @property rawsNumber - колчество строк;
 * @property colsNumber - количество столбцов в поле
 */
const config = {
    rawsNumber: Number(prompt('Задайте высоту игрового поля')),
    colsNumber: Number(prompt('Задайте ширину игрового поля'))
}